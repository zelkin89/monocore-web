---
import { Cpu } from 'lucide-react';
import '../styles/global.css';
---

<html lang="it">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="robots" content="noindex, nofollow" />
		<title>404 Error | MONOCORE™</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&family=Space+Grotesk:wght@400;500;700&display=swap" rel="stylesheet">
	</head>
    <body class="bg-void text-industrial antialiased selection:bg-brand-orange selection:text-void overflow-hidden">
        
        <div class="flex flex-col items-center justify-center min-h-screen w-full px-6 relative">
            
            {/* Background Grid Effect (Optional but cohesive) */}
            <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808008_1px,transparent_1px),linear-gradient(to_bottom,#80808008_1px,transparent_1px)] bg-[size:40px_40px] pointer-events-none"></div>

            {/* Icon */}
            <Cpu className="w-20 h-20 text-industrial/50 mb-8 z-10" strokeWidth={1.5} />

            {/* Error Code */}
            <h1 class="font-sans font-bold text-6xl md:text-9xl text-brand-orange tracking-widest leading-none mb-6 z-10 select-none">
                ERROR 404
            </h1>

            {/* Headline */}
            <h2 class="font-sans font-bold text-2xl md:text-4xl text-white uppercase tracking-tight mb-6 z-10">
                PERCORSO NON TROVATO
            </h2>

            {/* Body Copy */}
            <p class="font-mono text-industrial/80 text-sm md:text-base max-w-md mx-auto leading-relaxed mb-10 text-center z-10">
                Il sistema non ha mappato questo endpoint.<br/>
                Torna alla base operativa per riprendere il controllo.
            </p>

            {/* Primary CTA */}
            <a 
                href="/" 
                class="bg-brand-orange text-void font-sans font-bold px-8 py-4 rounded-sm hover:bg-white transition-all duration-300 hover:shadow-[0_0_20px_rgba(255,95,0,0.4)] z-10 uppercase tracking-wide inline-block"
            >
                TORNA ALLA HOME →
            </a>

            {/* Micro-copy Support */}
            <p class="text-xs font-mono text-industrial/40 mt-8 z-10">
                oppure <a href="mailto:support@monocore.it" class="hover:text-brand-orange underline underline-offset-2 transition-colors">contatta il supporto</a> se l'errore persiste
            </p>

        </div>

    </body>
</html>
